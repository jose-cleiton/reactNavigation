import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'red',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
  },
  textStyle: {
    color: 'white',
    fontSize: 24,
    fontWeight: 'bold',
  },
  // Mais estilos podem ser adicionados aqui conforme necessário
});
